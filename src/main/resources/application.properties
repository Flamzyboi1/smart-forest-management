# SensLog service URL. Override with SENSLOG_BASE_URL when running in Docker or on another host.
senslog.base-url=${SENSLOG_BASE_URL:http://localhost:8081}
senslog.connect-timeout-ms=${SENSLOG_CONNECT_TIMEOUT_MS:5000}
senslog.read-timeout-ms=${SENSLOG_READ_TIMEOUT_MS:15000}
